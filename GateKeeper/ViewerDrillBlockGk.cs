/// <summary>
/// Drill blocks shown in the GateKeeper viewer (expandable sections). Place recall is implemented;
/// hint / ridiculous story reuse the same shell in a later iteration.
/// </summary>
public enum ViewerDrillKind
{
	PlaceRecall,
	Hint,
	RidiculousStory,
}
