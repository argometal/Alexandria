
Cambio 232

Lista consolidada de pendientes para reemplazar la anterior: 
1) indicador visual del `role` en RAZ12,
 2) `Hint` como campo ligero del locus desplegable debajo del hero en franja/leyenda, 
 3) spacing dinámico entre frames según cantidad de assets `role='parcour'`, 
 4) tuning de spacing entre paneles `parcour`, ancho/alto final de panel y respiración del camino,
  5) decidir ocultar roof y quizá walls laterales, 
  6) menú ayuda/F1 actualizado en RAZ12 con hotkeys y menú desplegable,
   7) relanzar RAZ12 si se cierra durante sesión, 
   8) warp entre parcours, 
  9) limpieza de código muerto/refactor cuando pare la exploración visual.

Estado de pendientes ya parcialmente avanzados pero no cerrados visualmente: `hero` en frame ya funciona, `parcour` múltiple antes del frame ya funciona, pero ambos siguen en fase prototipo visual y no se consideran afinados.

Siguiente salto recomendado: `Enter Locus` real → teleport a Locus por `currentKey` → interfaz de objetos del locus usando lo aprendido de hero/parcour, dejando todo el tuning visual en esta lista congelada.

